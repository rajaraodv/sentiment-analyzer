# Sentiment Analyzer
It calls Github's GraphQL API to get list of issues opened by some user and analyzes their sentiment. PS: It only analyzes the main issue body (not comments).


<img src="screenshot.png" />

## Installation
This project has both the server(Express) and the client (React). The main root directory contains the Express sever, and the `/client` directory contains the React project.

### Steps
1. 